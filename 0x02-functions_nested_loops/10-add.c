#include "main.h"
#include <stdio.h>

/**
 * add - adds 2 arguments together
 * @x: is the first arg for the function
 * @y: is the 2nd arg for the function
 *
 * Return: the sum of the addition
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
