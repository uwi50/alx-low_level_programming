#include <stdio.h>

/**
 * mul - will multiply 2 integers
 * @a: is the first integer
 * @b: is the second integer
 *
 * Return: prod as the product made (a*b)
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
